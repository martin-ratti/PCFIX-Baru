---
import '../styles/global.css';
import { ClientRouter } from 'astro:transitions';

// 👇 COMPONENTES GLOBALES FLOTANTES
import ToastContainer from '../components/ui/feedback/ToastContainer'; 
import WhatsAppButton from '../components/ui/feedback/WhatsAppButton';

interface Props { 
    title: string;
    // 👇 Nuevas Props Opcionales para SEO (No rompen nada si no las pasas)
    description?: string;
    image?: string;
    price?: number;
}

const { 
    title, 
    description = "Tu tienda de confianza para hardware y tecnología.", 
    image = "/images/default-og.png", 
    price 
} = Astro.props;

// Lógica SEO: Construcción de URL absoluta para que WhatsApp detecte la imagen
const siteUrl = import.meta.env.SITE || 'https://pcfixbaru.com.ar';
const absoluteImageUrl = image.startsWith('http') ? image : `${siteUrl}${image}`;
const canonicalURL = new URL(Astro.url.pathname, siteUrl);
---

<!doctype html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/png" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        
        {/* SEO Básico */}
        <title>{title}</title>
        <meta name="description" content={description} />
        <link rel="canonical" href={canonicalURL} />

        {/* Open Graph (Facebook, WhatsApp) */}
        <meta property="og:type" content={price ? "product" : "website"} />
        <meta property="og:url" content={canonicalURL} />
        <meta property="og:title" content={title} />
        <meta property="og:description" content={description} />
        <meta property="og:image" content={absoluteImageUrl} />
        <meta property="og:site_name" content="PC FIX" />

        {/* Twitter Card */}
        <meta name="twitter:card" content="summary_large_image" />
        <meta name="twitter:title" content={title} />
        <meta name="twitter:description" content={description} />
        <meta name="twitter:image" content={absoluteImageUrl} />

        {/* Datos de Precio (Si es producto) */}
        {price && (
            <>
                <meta property="product:price:amount" content={price.toString()} />
                <meta property="product:price:currency" content="ARS" />
            </>
        )}

        <ClientRouter />
    </head>
    <body class="bg-gray-50 min-h-screen flex flex-col text-gray-900 selection:bg-primary/20">
        
        {/* 👇 NOTIFICACIONES Y CHAT FLOTANTE */}
        <ToastContainer client:load />
        <WhatsAppButton /> 

        <div class="flex-1">
            <slot />
        </div>
    </body>
</html>