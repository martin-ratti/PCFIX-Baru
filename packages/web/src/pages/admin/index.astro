---
export const prerender = false;

import Layout from "../../layouts/Layout.astro";

import AdminDashboard from "../../components/admin/core/AdminDashboard";
import AdminGuard from "../../components/guards/AdminGuard";
---

<Layout title="PCFIX | Administración">
  <main class="min-h-screen bg-gray-50 pt-10 pb-20">
    <div class="container mx-auto px-6">
      {/* 🛡️ Protegemos la ruta con el Guard */}
      <AdminGuard client:only="react">
        {
          /* El componente React maneja todo el contenido (Título, KPIs, Gráficos) */
        }
        <AdminDashboard client:load />
      </AdminGuard>
    </div>
  </main>
</Layout>
