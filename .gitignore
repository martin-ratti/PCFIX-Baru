# ## Dependencias e Instalaciones
# Ignora la carpeta que contiene todos los paquetes de npm
node_modules

# ## Logs
# Ignora logs de errores de npm y otros gestores de paquetes
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ## Archivos de Entorno
# Ignora archivos que contienen variables de entorno y secretos
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# ## Directorios de Build y Caché de Astro
# Ignora la carpeta de salida de la compilación y la caché de desarrollo
dist
.astro

# ## Archivos de Sistema Operativo
# Ignora archivos generados por macOS, Windows y Linux
.DS_Store
Thumbs.db
.AppleDouble
.LSOverride

# ## Configuración del Editor de Código
# Ignora las configuraciones específicas de VSCode
.vscode/*
!.vscode/extensions.json

# ## Backend Uploads
# Ignorar imágenes subidas localmente, pero mantener la carpeta si tiene un .gitkeep
packages/api/uploads/*
!packages/api/uploads/.gitkeep

# ## Prisma & Database
# Archivos de base de datos local si usaras SQLite (aunque usas Postgres, es buena práctica)
*.sqlite
*.sqlite-journal

# ## Entorno de ejecución
# Logs de depuración de errores
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# ## Test & Temp Files
# Archivos de salida de tests y depuración
coverage/
*.log
*-results.json
*-debug.json
*-timeout.json
test-results/
junit.xml

# ## Build Output (General)
build/
tmp/

